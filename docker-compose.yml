version: "3.2"

services:
  dev:
    container_name: node-template
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/liberland-node
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/liberland-node/.cargo
    volumes:
      - .:/var/www/liberland-node
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/liberland-node --dev --ws-external"
